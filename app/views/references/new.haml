%h1 Supply a reference!

= form_for([ @campaign, @application, @reference ]) do |f|

  %div{"id" => "contact"}
    %h2 Who are you?

    %p We need a little personal information

    - if @contact.errors.any?
      %div{"id" => "contact_errors"}
        %p We got problems!
    = render :partial => "contacts/form"

  %div{"id" => "reference_details"}
    %p Questions about your reference

  = render :partial => 'shared/questions', :locals => { :questions => @campaign.reference_questions }

  = f.submit "Submit"
