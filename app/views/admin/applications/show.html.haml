- add_subnavigation_item "Referenze-Formular", new_campaign_application_reference_url(@application.campaign, @application)
- add_subnavigation_item "bearbeiten", edit_admin_application_path(@application.id)

%h1= "Antrag von #{@application.contact.name}"

%h2 Kontakt
=render :partial => "admin/contacts/details", :locals => { :contact => @application.contact }

%h2 Antworten
= render :partial => "admin/answers/table", :locals => { :answers => @application.answers }

%h2 Referenzen
= render :partial => "admin/references/table", :locals => { :references => @application.references }

