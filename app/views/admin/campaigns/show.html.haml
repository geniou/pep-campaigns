%h1= "Kampagne #{@campaign.name}"

- add_subnavigation_item 'Fragen anzeigen', admin_campaign_questions_path(@campaign.id)
- add_subnavigation_item 'Bewerber-Formular', new_campaign_application_url(@campaign)
- add_subnavigation_item "bearbeiten", edit_admin_campaign_path(@campaign.id)

%p
  Required References per Application:
  = @campaign.required_reference_count

%h2 Anträge
= render :partial => "admin/applications/table", :locals => { :applications => @campaign.applications }
