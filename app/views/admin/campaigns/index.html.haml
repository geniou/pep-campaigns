%h1 Kampagnen

%p= link_to 'Kampagne anlegen', new_admin_campaign_path

- if @campaigns.size > 0
  %table.campaigns
    %thead
      %tr
        %th Name
    %tbody
      - @campaigns.each do |campaign|
        %tr
          %td= link_to campaign.name, admin_campaign_path(campaign.id)
- else
  %p Es gibt noch keine Kampagnen - leg doch eine an.

