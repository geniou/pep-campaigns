- if campaigns.size > 0
  %table.campaigns
    %thead
      %tr
        %th Name
        %th
        %th
        %th
    %tbody
      - campaigns.each do |campaign|
        %tr
          %td= campaign.name
          %td= link_to 'Fragen ansehen', admin_campaign_questions_path(campaign_id: campaign.id)
          %td= link_to 'Link für Bewerber', new_campaign_application_url(campaign)
          %td= link_to "bearbeiten", edit_admin_campaign_path(campaign.id)
- else
  %p None
