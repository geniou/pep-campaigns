%table.questions
  %thead
    %tr
      %th Frage
      %th
      %th
  %tbody
    - questions.each do |question|
      %tr
        %td= question.text
        %td= link_to 'bearbeiten', edit_admin_campaign_question_path(id: question.id, campaign_id: question.campaign.id)
        %td= button_to 'löschen', admin_campaign_question_path(id: question.id, campaign_id: question.campaign.id), confirm: 'Sicher?', method: :delete
